import order_updated from "./order_updated.js";

export default async (req, res) => {
  // res.sendStatus(200);

  order_updated(req, res);

  // const order = req.body;

  // console.log(order);

  // if (!order.note) return;

  // Shopify.order
};
